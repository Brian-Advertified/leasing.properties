# Apps Workspace

Target workspace for deployable applications.

- `apps/web`: VodaPay-ready React mini-app shell.
- `apps/api`: modular API service for bookings, leases, payments, rewards, and trust systems.

The current prototype still runs from the repository root. Move code into these folders once TypeScript, route boundaries, and shared package wiring are introduced.
